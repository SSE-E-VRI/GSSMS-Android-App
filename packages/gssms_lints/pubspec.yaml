name: gssms_lints
description: Custom lints that keep GSSMS Mobile on design-system tokens.
publish_to: none
version: 0.0.1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  analyzer: '>=6.0.0 <7.0.0'
  analyzer_plugin: '>=0.11.0 <0.12.0'
  custom_lint_builder: ^0.6.4
